describe(__filename, function () {
    before(async function () {});


    after(async function () {});


    it('should return pong', async function () {
        console.log(this.discord, Object.keys(this.discord));
        this.expect(true);
    });
});
